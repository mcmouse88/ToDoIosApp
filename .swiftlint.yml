# SwiftLint configuration — https://github.com/realm/SwiftLint
# Gentle starter config so the current skeleton passes. Tighten over time.

included:
  - ToDoApp

disabled_rules:
  - trailing_whitespace
  - todo

line_length:
  warning: 120
  error: 200

identifier_name:
  min_length: 2
